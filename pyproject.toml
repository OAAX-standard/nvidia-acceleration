[project]
name = "nvidia-acceleration-dev"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pytest",
    "onnx",
    "ultralytics",
]

[tool.pytest.ini_options]
pythonpath = ["."]
